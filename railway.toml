[build]
builder = "nixpacks"

[deploy]
startCommand = "python init_sprint1_db.py && uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

